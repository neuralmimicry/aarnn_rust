// Copyright NeuralMimicry. All Rights Reserved.
//
// Module entry point for the NmAerBridge runtime module. Declared as a plain
// (non-primary) module so the folder can be dropped verbatim into any Unreal
// project's Source/ tree without clashing with that project's game module.

#include "Modules/ModuleManager.h"

IMPLEMENT_MODULE(FDefaultModuleImpl, NmAerBridge);
