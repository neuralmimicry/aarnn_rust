[/Script/EngineSettings.GameMapsSettings]
; Auto-populate any level (including PIE on an empty map) with AARNN robots.
; run_sim.sh also forces this GameMode via the launch URL for standalone -game.
GlobalDefaultGameMode=/Script/NmAerBridge.NmSimGameMode
GameDefaultMap=/Engine/Maps/Templates/Template_Default
EditorStartupMap=/Engine/Maps/Templates/Template_Default

[/Script/Engine.Engine]
; Keep networking drivers at engine defaults; the AARNN link is a plain TCP
; socket owned by the NmAerBridge module, not an Unreal net driver.
