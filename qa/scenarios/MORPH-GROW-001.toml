id = "MORPH-GROW-001"
version = 1
title = "Bounded growth-cone proposals and finite-width admission"
requirements = ["DATA-04", "ENV-02", "ENV-03", "GROW-02", "GROW-05", "GROW-07", "QA-01"]
products = ["server", "workstation"]
mode = "headless reference policy; live structural activation disabled"
seed = 42
logical_stop = "twenty independent cone steps and eight reference geometry commits"
oracle = "same-stream replay; bounded turn/length; no thin-wall tunnelling or capsule overlap outside declared parent joints; explicit stalls and deferrals"
required_artifacts = ["result.json", "run.log"]
model_fixture_digest = "sha256:src/morphology_contract/growth_cone.rs; recorded per run"
config_fixture_digest = "sha256:qa/fixtures/morphology/growth-cone-v1.json; recorded per run"
input_fixture_digest = "sha256:qa/scenarios/MORPH-GROW-001.toml; recorded per run"
target_triples = ["x86_64-unknown-linux-gnu"]
capabilities = ["cargo", "python3"]
device_requirement = "CPU reference tests; no renderer, peripherals or live brain"
resource_bounds = "64 candidates, 4096 capsules, 1024 obstacles, 256 history entries; excess defers"
latency_bounds = "600 seconds build/test; no stimulus latency claim"
reference_profile = "Procedural f64 geometry, exact same-host replay and 1e-12 mm local test tolerance; no cross-platform bitwise claim"
expected_digest_procedure = "repeat stable brain/tip/generation/step coordinates with reordered occupancy and unrelated RNG draws"
admission_loss_policy = "stall or defer unchanged tip; revalidate proposal base and stored swept path before reference admission"
