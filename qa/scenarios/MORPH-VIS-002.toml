id = "MORPH-VIS-002"
version = 1
title = "Shipped-network anatomical stability through peripheral formation"
requirements = ["CORE-03", "DATA-03", "VIS-01", "UI-01", "UI-03", "RUN-02", "QA-04"]
products = ["workstation", "web"]
mode = "offline CPU, synchronous growth; no live brain"
seed = 42
logical_stop = "1,200 steps, 13 snapshots, including 500 and 1,000 ms development boundaries"
oracle = "exact old arbors on direct I/O resize; protected roots survive real growth/admission steps with local movement at most 0.05 fixture units while leaf pruning continues; valid snapshots; stable meshes/pixels for repeated frames; membrane-contained mesh vertices"
required_artifacts = ["result.json", "run.log", "growth-metrics.json", "frame-*.json", "synthetic-*.json", "native-growth-*.svg", "web-growth.png"]
model_fixture_digest = "sha256:config.json; recorded per run"
config_fixture_digest = "sha256:qa/scenarios/MORPH-VIS-002.toml; recorded per run"
input_fixture_digest = "sha256:src/engine.rs; recorded per run"
target_triples = ["x86_64-unknown-linux-gnu"]
capabilities = ["cargo", "nodejs", "google-chrome"]
device_requirement = "Native CPU mesh builder and actual Chrome canvas. Native GPU, iOS and Android device evidence are separate gates."
resource_bounds = "Shipped 210 hidden-cell initial population; 512 node and 4,096 path display budgets; synchronous test only"
latency_bounds = "300 seconds per build/capture; 120 seconds native mesh; 90 seconds browser lane; not the QA-05 contention benchmark"
reference_profile = "Legacy procedural AARNN growth, existing numerical configuration; no biophysical accuracy claim"
expected_digest_procedure = "byte equality of old arbors on direct I/O resize; same parallel geometry at one and four workers; repeat mesh and pixel equality per captured state"
admission_loss_policy = "Only derived display clipping/truncation; no causal event admission changes"
