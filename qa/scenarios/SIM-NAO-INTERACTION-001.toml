id = "SIM-NAO-INTERACTION-001"
version = 1
title = "NAO social reference: player input, autonomous encounters and neural output"
requirements = ["INV-002", "INV-007", "INV-015", "INV-016", "INV-017", "spec-19.3", "spec-21.13"]
products = ["host", "workstation", "web"]
mode = "opt-in legacy reference; production workstation_io disabled"
seed = 0
logical_stop = "nine observed Rust speech acts; bounded denial/cancellation cases"
oracle = "exact act identity and preserved original matrices; no response on silence; player/writer isolation"
required_artifacts = ["result.json", "unit.log for contract lane", "launcher.log", "runtime/rust.log", "browser.log and screenshots when requested", "engine.log or native.log for native lanes"]
model_fixture_digest = "generated model SHA-256 retained in result.json; source network_nao.json unchanged"
config_fixture_digest = "sim/nao/interaction.json SHA-256 retained per run"
input_fixture_digest = "scripts/qa/run_nao_social.py versioned nine-case fixture"
target_triples = ["x86_64-unknown-linux-gnu"]
capabilities = ["python3", "Rust nn_tcp_server", "loopback", "optional Playwright Chromium", "Webots/Unreal/BDS required only for their explicit native lanes"]
device_requirement = "no physical robot or microphone; browser STT mocked; native engine lanes separate"
resource_bounds = "16 players, 8 queued turns, 256 UTF-8 bytes per turn, 64 KiB frames, 8 HTTP workers, one body writer"
latency_bounds = "180 s startup, 320 neural frames per turn, 128 presentation guard frames, browser 180 s"
reference_profile = "nao_social_v1: engineered LIF reflexes; 282/49; original 250/40 matrices retained"
expected_digest_procedure = "no biological adequacy claim; retain actual neural output channel, count and reference step for every act"
admission_loss_policy = "reject before admission; no automatic retry after ambiguous exchange; cancel presentation on disconnect; no production commit certificate"
