id = "EX-001"
version = 1
title = "Small deterministic neuron/synapse network"
requirements = ["UT-ORDER-001", "UT-NUM-001", "INV-008", "APP-INV-001"]
products = ["host", "web", "ios-simulator", "android-emulator"]
mode = "deterministic-reference"
seed = 1
logical_stop = "fixed_event_count"
oracle = "exact event/state/output digest equals host reference"
required_artifacts = ["result.json", "state-digest", "event-digest"]
model_fixture_digest = "builtin:deterministic-reference-network-v1"
config_fixture_digest = "builtin:NetworkConfig-default-v1"
input_fixture_digest = "builtin:zero-sensory-input-v1"
target_triples = ["x86_64-unknown-linux-gnu"]
capabilities = ["cpu", "deterministic-reference"]
device_requirement = "host-or-supported-simulator"
resource_bounds = "bounded fixture profile; no unbounded queues"
latency_bounds = "not a real-time claim; record wall time"
reference_profile = "DeterministicReference"
expected_digest_procedure = "compare exact host state/event/output digests"
admission_loss_policy = "no post-admission loss; no pre-admission coalescing"
