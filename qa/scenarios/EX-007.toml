id = "EX-007"
version = 1
title = "Synthetic bidirectional USB-AER loopback"
requirements = ["UT-AERUSB-001", "UT-AERUSB-002", "CT-016", "INV-015", "INV-017"]
products = ["host", "workstation", "android"]
mode = "synthetic-peripheral"
seed = 7
logical_stop = "fixed_frame_count"
oracle = "bounded frames, independent channel queues and exactly-once effects"
required_artifacts = ["result.json", "aer-frame-digest", "gap-records"]
model_fixture_digest = "builtin:synthetic-aer-loopback-v1"
config_fixture_digest = "builtin:duplex-channel-budgets-v1"
input_fixture_digest = "builtin:recorded-aer-frames-v1"
target_triples = ["x86_64-unknown-linux-gnu"]
capabilities = ["cpu", "aer-reference", "bounded-duplex-channels"]
device_requirement = "host-fake; physical workstation/Android lane separately required"
resource_bounds = "bounded frames, credits, payloads and per-direction queues"
latency_bounds = "record transport latency; no real-time claim"
reference_profile = "AARNN-AER/1-Reference"
expected_digest_procedure = "verify frame CRC, ordering, gaps and effect dedupe"
admission_loss_policy = "pre-admission loss emits gap/quality records; admitted frames are retained"
