id = "EX-012"
version = 1
title = "Cross-process durable failover and fenced rejoin"
requirements = ["CT-006", "CT-007", "INV-001", "INV-012"]
products = ["host", "server", "orchestrator"]
mode = "cross-process-fault-injection"
seed = 12
logical_stop = "replacement-commit-and-rejoin-decision"
oracle = "replacement restores exact warm boundary; stale active cannot commit"
required_artifacts = ["result.json", "recovery-evidence", "rpo-rto-measurement"]
model_fixture_digest = "builtin:default-lif-reference-v1"
config_fixture_digest = "builtin:cross-process-failover-v1"
input_fixture_digest = "builtin:one-commit-then-fenced-retry-v1"
target_triples = ["x86_64-unknown-linux-gnu"]
capabilities = ["cpu", "filesystem-locking", "majority-replica-reference"]
device_requirement = "host-processes; networked consensus and physical failure domains separately required"
resource_bounds = "bounded child process, durable WAL/checkpoint and three authority replicas"
latency_bounds = "record monotonic recovery wall time; no production RTO claim"
reference_profile = "LocalDurableFailoverReference"
expected_digest_procedure = "compare replacement snapshot/checkpoint digest and verify stale-writer rejection"
admission_loss_policy = "only durable committed prefix is recovered; unresolved work is explicit"
