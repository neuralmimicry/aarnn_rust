id = "EX-002"
version = 1
title = "Superdense cascade and delayed feedback"
requirements = ["UT-TIME-002", "UT-SCC-001", "INV-003", "INV-004"]
products = ["host", "server"]
mode = "deterministic-reference"
seed = 2
logical_stop = "fixed_event_count"
oracle = "zero-delay increments microstep; positive delay resets microstep"
required_artifacts = ["result.json", "event-digest"]
model_fixture_digest = "builtin:superdense-cascade-v1"
config_fixture_digest = "builtin:logical-time-boundaries-v1"
input_fixture_digest = "builtin:causal-event-sequence-v1"
target_triples = ["x86_64-unknown-linux-gnu"]
capabilities = ["cpu", "superdense-executor"]
device_requirement = "host-reference"
resource_bounds = "bounded event and settling queues"
latency_bounds = "not a real-time claim; record wall time"
reference_profile = "DeterministicReference"
expected_digest_procedure = "compare exact logical-tag and event digests"
admission_loss_policy = "no post-admission loss; unresolved work is retained"
