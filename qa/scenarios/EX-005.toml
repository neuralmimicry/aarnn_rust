id = "EX-005"
version = 1
title = "Independent brains and explicit federation"
requirements = ["APP-INV-003", "APP-INV-005", "INV-010"]
products = ["host", "orchestrator", "android", "ios"]
mode = "federation-lab"
seed = 5
logical_stop = "explicit_link_revoke"
oracle = "separate identities and positive-delay link"
required_artifacts = ["result.json", "link-audit"]
model_fixture_digest = "builtin:independent-brains-v1"
config_fixture_digest = "builtin:federation-link-policy-v1"
input_fixture_digest = "builtin:versioned-link-events-v1"
target_triples = ["x86_64-unknown-linux-gnu"]
capabilities = ["cpu", "management", "federation-reference"]
device_requirement = "host-reference; native clients separately gated"
resource_bounds = "bounded per-direction credits and link queues"
latency_bounds = "record link mapping and observed wall time; no real-time claim"
reference_profile = "IndependentBrainFederationReference"
expected_digest_procedure = "verify independent identities and positive-delay link audit"
admission_loss_policy = "link-local gaps are explicit and never silently replayed"
