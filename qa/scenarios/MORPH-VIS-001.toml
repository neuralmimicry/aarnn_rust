id = "MORPH-VIS-001"
version = 1
title = "Stable anatomical projection, local tube meshes and membrane containment"
requirements = ["CORE-03", "VIS-01", "VIS-09", "VIS-10", "UI-01", "UI-03", "QA-04"]
products = ["workstation", "web"]
mode = "read-only mesh fixture; bounded procedural growth regression"
seed = 0
logical_stop = "fixed fixture; 98 unchanged native snapshots, 20 identical browser frames, eight growth intervals"
oracle = "exact repeated meshes/pixels; no triangles bridging a bend; all rendered vertices inside the published membrane"
required_artifacts = ["result.json", "run.log", "native-mesh.svg", "web-canvas.png when browser requested", "browser-result.txt when browser requested"]
model_fixture_digest = "sha256:qa/fixtures/morphology/anatomical-stability.json; recorded per run"
config_fixture_digest = "sha256:qa/scenarios/MORPH-VIS-001.toml; recorded per run"
input_fixture_digest = "sha256:scripts/qa/test_anatomical_render.cjs; recorded per run"
target_triples = ["x86_64-unknown-linux-gnu"]
capabilities = ["cargo", "nodejs", "google-chrome for browser lane"]
device_requirement = "No live brain or peripherals. Native evidence is the actual CPU mesh builder, not a native GPU window. Mobile SDKs remain separate gates."
resource_bounds = "four somas, ten paths, thirteen samples per connection; browser pixel oracle 800x600; bounded tests"
latency_bounds = "300 seconds build/test; 30 seconds Chrome; no stimulus latency claim"
reference_profile = "Procedural display fixture; legacy growth numerical regression"
expected_digest_procedure = "repeat identical geometry with changed sequence and truncation; compare native meshes and browser pixels exactly"
admission_loss_policy = "Read-only display clipping never modifies physical routes or drops causal events"
