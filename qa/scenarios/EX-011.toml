id = "EX-011"
version = 1
title = "Offline standalone brain checkpoint and restore"
requirements = ["APP-INV-001", "APP-INV-006", "MOB-E2E-013"]
products = ["host", "ios-simulator", "android-emulator"]
mode = "standalone-brain"
seed = 11
logical_stop = "checkpoint_then_forced_termination"
oracle = "restored digest equals host and suspension advances no biological time"
required_artifacts = ["result.json", "checkpoint-digest", "restore-digest"]
model_fixture_digest = "builtin:standalone-brain-v1"
config_fixture_digest = "builtin:mobile-budgeted-reference-v1"
input_fixture_digest = "builtin:offline-input-v1"
target_triples = ["x86_64-unknown-linux-gnu"]
capabilities = ["cpu", "encrypted-checkpoint-reference", "offline"]
device_requirement = "host-reference; iOS/Android simulator lanes separately required"
resource_bounds = "configured neuron, event, queue and checkpoint budgets"
latency_bounds = "record checkpoint/recovery wall time; no continuous-execution claim"
reference_profile = "StandaloneBrainReference"
expected_digest_procedure = "compare restored host state digest and explicit uncommitted interval"
admission_loss_policy = "suspension advances no biological time; uncommitted work is reported"
